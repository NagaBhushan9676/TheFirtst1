<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <style>
        body{
            display: flex;
            flex-direction: column;
            align-items: center;
        }
        h1{
            color: fuchsia;
        }
        #res{
            color: blueviolet;
        }
        h2{
            color: aquamarine;
        }
    </style>
</head>
<body>
    <%- include('navbar')%>
    <h1>Orey Bammardi idigo Random Number <%= rand  %> </h1>
    <h1 id="res"><%= rand % 2 === 0 ? 'EVEN NUMBER' : 'ODD NUMBER'%></h1>
    <% if(rand % 2 === 0) { %>
        <h2>Even</h2> 
        <% } else { %>
         <h2 >Odd</h2>
         <% } %>
</body>
</html>